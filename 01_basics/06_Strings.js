const name="Prateek"

const repoCount=50
//OUTDATED method
// console.log(name+repoCount+"value")


//string interpolation as is can directly assign value to placeholders
console.log(`Hello my name is ${name} and my repo count is ${repoCount}`)